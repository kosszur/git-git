`badDate`::
	(ERROR) Érvénytelen dátumformátum egy author/committer sorban.

`badDateOverflow`::
	(ERROR) Érvénytelen dátumérték egy author/committer sorban.

`badEmail`::
	(ERROR) Érvénytelen e-mail-formátum egy author/committer sorban.

`badFilemode`::
	(INFO) Egy fa hibás filemode-bejegyzést tartalmaz.

`badGpgsig`::
	(ERROR) Egy tag hibás (csonka) aláírásfejlécet (pl. `gpgsig`) tartalmaz.

`badHeadTarget`::
	(ERROR) A `HEAD` ref olyan symref, amely nem branchre hivatkozik.

`badHeaderContinuation`::
	(ERROR) Egy folytatásfejléc (például a `gpgsig`-hez) váratlanul csonka.

`badName`::
	(ERROR) Egy author/committer név üres.

`badObjectSha1`::
	(ERROR) Egy objektumnak hibás a sha1-e.

`badPackedRefEntry`::
	(ERROR) A "packed-refs" fájl érvénytelen bejegyzést tartalmaz.

`badPackedRefHeader`::
	(ERROR) A "packed-refs" fájl érvénytelen
	fejlécet tartalmaz.

`badParentSha1`::
	(ERROR) Egy commit-objektumnak hibás a szülő-sha1-e.

`badRefContent`::
	(ERROR) Egy refnek hibás a tartalma.

`badRefFiletype`::
	(ERROR) Egy refnek hibás a fájltípusa.

`badRefName`::
	(ERROR) Egy ref formátuma érvénytelen.

`badRefOid`::
	(ERROR) Egy ref érvénytelen objektumazonosítóra mutat.

`badReferentName`::
	(ERROR) Egy symref hivatkozott neve érvénytelen.

`badReftableTableName`::
	(WARN) Egy reftable tábla neve érvénytelen.

`badTagName`::
	(INFO) Egy tag formátuma érvénytelen.

`badTimezone`::
	(ERROR) Érvénytelen időzóna egy author/committer sorban.

`badTree`::
	(ERROR) Egy fa nem elemezhető.

`badTreeSha1`::
	(ERROR) Egy fa formátuma érvénytelen.

`badType`::
	(ERROR) Érvénytelen objektumtípus.

`duplicateEntries`::
	(ERROR) Egy fa duplikált fájlbejegyzéseket tartalmaz.

`emptyName`::
	(WARN) Egy elérési út üres nevet tartalmaz.

`emptyPackedRefsFile`::
	(INFO) A "packed-refs" fájl üres. Ha ezt a hibát látod, jelezd a
	git@vger.kernel.org levelezőlistán. Mivel csak a Git nagyon korai
	verziói hoztak létre ilyen üres "packed_refs" fájlt, a jövőben
	szigoríthatunk ezen a szabályon.

`extraHeaderEntry`::
	(IGNORE) A `tagger` után extra fejlécek találhatók.

`fullPathname`::
	(WARN) Egy elérési út a "/"-rel kezdődő teljes elérési utat tartalmazza.

`gitattributesBlob`::
	(ERROR) Nem blob található a `.gitattributes` helyén.

`gitattributesLarge`::
	(ERROR) A `.gitattributes` blob túl nagy.

`gitattributesLineLength`::
	(ERROR) A `.gitattributes` blob túl hosszú sorokat tartalmaz.

`gitattributesMissing`::
	(ERROR) A `.gitattributes` blob nem olvasható.

`gitattributesSymlink`::
	(INFO) A `.gitattributes` egy symlink.

`gitignoreSymlink`::
	(INFO) A `.gitignore` egy symlink.

`gitmodulesBlob`::
	(ERROR) Nem blob található a `.gitmodules` helyén.

`gitmodulesLarge`::
	(ERROR) A `.gitmodules` fájl túl nagy az elemzéshez.

`gitmodulesMissing`::
	(ERROR) A `.gitmodules` blob nem olvasható.

`gitmodulesName`::
	(ERROR) Egy submodule-név érvénytelen.

`gitmodulesParse`::
	(INFO) A `.gitmodules` blob nem volt elemezhető.

`gitmodulesPath`::
	(ERROR) A `.gitmodules` elérési útja érvénytelen.

`gitmodulesSymlink`::
	(ERROR) A `.gitmodules` egy symlink.

`gitmodulesUpdate`::
	(ERROR) Érvénytelen submodule-frissítési beállítás.

`gitmodulesUrl`::
	(ERROR) Érvénytelen submodule-url.

`hasDot`::
	(WARN) Egy fa `.` nevű bejegyzést tartalmaz.

`hasDotdot`::
	(WARN) Egy fa `..` nevű bejegyzést tartalmaz.

`hasDotgit`::
	(WARN) Egy fa `.git` nevű bejegyzést tartalmaz.

`largePathname`::
	(WARN) Egy fa nagyon hosszú elérésiút-nevet tartalmazó bejegyzést
	tartalmaz. Ha a `fsck.largePathname` értéke kettőspontot tartalmaz,
	azt az értéket használja a megengedett maximális hosszként (pl. a
	"warn:10" minden 11 vagy több bájtos útvonalkomponensre panaszkodna).
	Az alapértelmezett érték 4096.

`mailmapSymlink`::
	(INFO) A `.mailmap` egy symlink.

`missingAuthor`::
	(ERROR) Hiányzik az author.

`missingCommitter`::
	(ERROR) Hiányzik a committer.

`missingEmail`::
	(ERROR) Hiányzik az e-mail egy author/committer sorban.

`missingNameBeforeEmail`::
	(ERROR) Hiányzik a név egy e-mail előtt egy author/committer sorban.

`missingObject`::
	(ERROR) Hiányzik az `object` sor a tag-objektumból.

`missingSpaceBeforeDate`::
	(ERROR) Hiányzik a szóköz a dátum előtt egy author/committer sorban.

`missingSpaceBeforeEmail`::
	(ERROR) Hiányzik a szóköz az e-mail előtt egy author/committer sorban.

`missingTag`::
	(ERROR) Váratlan vég a `type` sor után egy tag-objektumban.

`missingTagEntry`::
	(ERROR) Hiányzik a `tag` sor egy tag-objektumból.

`missingTaggerEntry`::
	(INFO) Hiányzik a `tagger` sor egy tag-objektumból.

`missingTree`::
	(ERROR) Hiányzik a `tree` sor egy commit-objektumból.

`missingType`::
	(ERROR) Érvénytelen típusérték a `type` sorban egy tag-objektumban.

`missingTypeEntry`::
	(ERROR) Hiányzik a `type` sor egy tag-objektumból.

`multipleAuthors`::
	(ERROR) Több author sor található egy commitban.

`nulInCommit`::
	(WARN) NUL bájt található a commit-objektum törzsében.

`nulInHeader`::
	(FATAL) NUL bájt van az objektumfejlécben.

`nullSha1`::
	(WARN) A fa null sha1-re mutató bejegyzéseket tartalmaz.

`packedRefEntryNotTerminated`::
	(ERROR) A "packed-refs" fájl olyan bejegyzést tartalmaz, amelyet
	nem zár le újsor.

`packedRefUnsorted`::
	(ERROR) A "packed-refs" fájl nincs rendezve.

`refMissingNewline`::
	(INFO) Egy laza ref, amely nem újsorral (LF) végződik. Mivel a Git
	érvényes implementációi sosem hoztak létre ilyen laza ref-fájlt, ez a
	jövőben hibává válhat. Ha ezt a hibát látod, jelezd a
	git@vger.kernel.org levelezőlistán, mert tudnunk kell, mely eszközök
	hoztak létre ilyen fájlt.

`symlinkRef`::
	(INFO) Egy szimbolikus link van symrefként használva. Ha ezt a hibát
	látod, jelezd a git@vger.kernel.org levelezőlistán, mert éppen azt
	mérjük fel, megvalósítható-e a szimbolikus linkek symrefként való
	létrehozásának megszüntetése.

`symrefTargetIsNotARef`::
	(INFO) Egy szimbolikus referencia célja sem gyökérreferenciára, sem
	"refs/"-fel kezdődő referenciára nem mutat. Bár a `git symbolic-ref`
	használatával megengedjük olyan symref létrehozását, amely a "ref"-en
	kívüli hivatkozottra mutat, a jövőben szigoríthatunk a szabályon. Ha
	ezt a hibát látod, jelezd a git@vger.kernel.org levelezőlistán, mert
	tudnunk kell, mely eszközök hoztak létre ilyen fájlt.

`trailingRefContent`::
	(INFO) Egy laza refnek záró tartalma van. Mivel a Git érvényes
	implementációi sosem hoztak létre ilyen laza ref-fájlt, ez a jövőben
	hibává válhat. Ha ezt a hibát látod, jelezd a git@vger.kernel.org
	levelezőlistán, mert tudnunk kell, mely eszközök hoztak létre ilyen
	fájlt.

`treeNotSorted`::
	(ERROR) Egy fa nincs megfelelően rendezve.

`unknownType`::
	(ERROR) Ismeretlen objektumtípus.

`unterminatedHeader`::
	(FATAL) Hiányzik a sorvég az objektumfejlécben.

`zeroPaddedDate`::
	(ERROR) Nullákkal feltöltött dátum egy author/committer sorban.

`zeroPaddedFilemode`::
	(WARN) Nullákkal feltöltött filemode egy fában.
